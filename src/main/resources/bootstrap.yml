spring:
  application:
    name: bank-service
  profiles:
    active: dev

  config:
    name: bank-service
  cloud:
    config:
      uri: http://localhost:8888/
      username: own_username
      password: own_password